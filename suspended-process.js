//const uint32 = ref.types.uint32;
// const kernel32 = ffi.Library("kernel32", {
//   SuspendThread: [uint32, ["pointer"]],
// });

// console.log("Suspending thread...");
// const suspendResult = kernel32.SuspendThread(pi.hThread);

// if (suspendResult === 0xffffffff) {
//   console.log("Failed to suspend thread.");
//   throw new Error("SuspendThread failed.");
// }
// console.log("Thread suspended successfully.");
